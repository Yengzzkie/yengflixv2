import styled from "styled-components"

const Nav = styled.nav`
    display: flex;
    align-items: center;
    border: 1px solid #fff;
    width: 100vw;
    height: 60px;
    padding: 1rem;
  `

export default Nav;